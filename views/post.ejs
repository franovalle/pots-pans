<!--Note to self: this is part of the post route-->
<!--Note to self: Using this repo as a guide: https://github.com/Resilient-Labs/quick-pound-2025a-->

<%- include('partials/header') -%>
 
  <div class="container">
    <div class="row justify-content-center mt-5">
      <div class="border border-danger">
         <!--<span class ="h2">
    <%= user.userName %>
  </span>-->
       <span class = "h3">Hospital Name:</span>
        <span class="h3"> <%= post.org %></span>
        <br></br>
        <span class = "h3">Item:</span>
        <span class="h3"><%= post.item %></span>
         <br></br>
        <span class = "h3">Description:</span>
        <span class="h3"><%= post.description %></span>
         <br></br>
         <span class = "h3">Note:</span>
         <span class="h3"><%= post.note %></span>
         <br></br>
         <span class = "h3">Delivery Date:</span>
        <span class="h3"> <%= post.deliveryDate %></span>
         <br></br>
         <span class = "h3">Delivery Time:</span>
        <span class="h3"> <%= post.deliveryTime %></span>
        <!--<img class="img-fluid" src="<%= post.image%>" />
        <div class="row justify-content-between">
          <form class="col-1" action="/post/likePost/<%= post.id %>?_method=PUT" method="POST">
            <button class="btn btn-primary fa fa-heart" type="submit"></button>
          </form>
          <h3 class="col-3">Likes: <%= post.likes %>
          </h3>-->
         <%if(post.user == user.id){ %>
        <form
          action="/post/deletePost/<%= post.id %>?_method=DELETE"
          method="POST"
          class="col-3"
        >
          <button class="btn btn-danger fa fa-trash" type="submit"></button>
        </form>
        <%}%>
            
            
            
        </div>
      </div>
    
    
  

  <%- include('partials/footer') -%>

